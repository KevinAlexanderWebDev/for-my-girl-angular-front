import { PipecustomDatePipePipe } from './pipecustom-date.pipe-pipe';

describe('PipecustomDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new PipecustomDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
